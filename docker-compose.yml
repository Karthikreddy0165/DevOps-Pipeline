version: '3.8'
services:
  app:
    build: .
    ports:
      - '3000:3000'
    env_file:
      - .env
    volumes:
      # Persist the SQLite database file across container restarts
      - sqlite_data:/app/data
    environment:
      - NODE_ENV=production

volumes:
  sqlite_data:
